scalar BigDecimal
scalar DateTime

schema {
    query: Query
    mutation: Mutation
}

type PageInfo {
    hasPreviousPage: Boolean!
    hasNextPage: Boolean!
}

enum MutationAction {
    ATTACH
    DETACH
}