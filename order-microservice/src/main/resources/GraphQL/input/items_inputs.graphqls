input ItemAndItemInput {
    id: ID
    orderId: ID!
    parentId: ID!
    childId: ID!
    quantity: Float
}

input ItemInput {
    id: ID!
    name: String
    description: String
    material: String
    quantity: BigDecimal
}