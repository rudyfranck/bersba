input ParameterInput {
    #this fields is mandatory when editing parameter
    id: ID
    name: String
    description: String
    value: String
    #this fields is mandatory when creating new parameter
    type: ParameterType
    #this fields is mandatory when creating new parameter
    hostId: ID
    #this fields is mandatory when creating new parameter
    orderId: ID
}
